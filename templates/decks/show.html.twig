
<title>Deck</title>

<h1>Deck : {{ deck.name }}</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ deck.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td></td>
        </tr>
    </tbody>
</table>

<a href="{{ path('list-deck') }}">Retour aux decks</a>

<a href="{{ path('edit-deck', {'id': deck.id}) }}">Modifier</a>

{{ include('decks/_delete_form.html.twig') }}

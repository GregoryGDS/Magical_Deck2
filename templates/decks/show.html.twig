{% extends 'base.html.twig' %}

{% block title %}Deck{% endblock %}

{% block body %}
    <h1>Deck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deck.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ deck.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list-deck') }}">back to list</a>

    <a href="{{ path('edit-deck', {'id': deck.id}) }}">edit</a>

    {{ include('decks/_delete_form.html.twig') }}
{% endblock %}
